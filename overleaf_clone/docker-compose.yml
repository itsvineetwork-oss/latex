version: '3.8'
services:
  app:
    build:
      context: ./overleaf_clone
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./overleaf_clone/frontend:/app/frontend:ro
      - ./overleaf_clone/backend:/app:ro
    restart: unless-stopped
